using Character;
using Character.Player;
using ID;

namespace Buff
{
    public class BuffEffect
    {
        
        public ObjectIndex ObjectIndex = new ObjectIndex();
        public StatsBase TotalEffects = new StatsBase();
        public StatsBase IntervalEffect = new StatsBase();
    }
}